{
  "name": "recue",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run:manager": "npm run dev --prefix manager",
    "run:player": "npm run dev --prefix player",
    "run:server": "npm run dev --prefix server",
    "build:manager": "npm run build --prefix manager",
    "build:player": "npm run build --prefix player"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^40.1.0"
  }
}