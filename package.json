{
  "name": "recue",
  "version": "1.1.4",
  "main": "electron/main/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run:manager": "npm run dev --prefix manager",
    "run:player": "npm run dev --prefix player",
    "run:server": "npm run dev --prefix server",
    "start": "electron . --no-sandbox",
    "run:electron": "npm run start",
    "build:manager": "npm run build --prefix manager",
    "build:player": "npm run build --prefix player",
    "build:all": "npm run build:manager && npm run build:player",
    "build:electron": "npm install && npm run build:all && electron-builder",
    "package": "npm run build:all && electron-builder",
    "bump:patch": "npm version patch",
    "bump:minor": "npm version minor",
    "bump:major": "npm version major"
  },
  "author": "Algosake <recue@root.ee>",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^40.1.0",
    "electron-builder": "^25.0.0",
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/multipart": "^9.4.0",
    "@fastify/static": "^9.0.0",
    "@fastify/websocket": "^11.2.0",
    "electron-store": "^8.1.0",
    "fastify": "^5.7.4",
    "ffmpeg-static": "^5.3.0",
    "nodemon": "^3.1.11",
    "osc": "^2.4.5"
  }
}